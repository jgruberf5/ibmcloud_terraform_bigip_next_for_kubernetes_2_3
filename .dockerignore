.git
.gitignore
.claude/
.dockerignore
Dockerfile

# Regenerated by `terraform init` in the build
.terraform/
.terraform-provider-cache/

# State and per-user secrets must never leak into the image
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
terraform.tfvars

# run_tests.sh outputs
test-runs/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
