# Terraform state — may contain secrets
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Terraform working directory
.terraform/
.terraform.lock.hcl
.terraform-provider-cache/
remove_terraform_state

# Environment files
.env
.env.*
make_dot_envs.sh

# run_tests.sh output trees
test-runs/
test-reports/

# IBM Cloud provider kubeconfig downloads (contain cluster auth tokens)
*_k8sconfig/

# Variable files that contain secrets (API keys, passwords, etc.)
terraform.tfvars

# Claude Code session data
.claude/

# OS
.DS_Store
Thumbs.db
